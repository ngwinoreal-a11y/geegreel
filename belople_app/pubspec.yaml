name: belople_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # --- Phase 0: app shell / state / theming ---
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  go_router: ^14.8.1
  google_fonts: ^6.2.1

  # --- Phase 1+: networking, storage (wired up now so the skeleton compiles
  # against the real choices from the plan; used starting Phase 1/2) ---
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
  video_player: ^2.9.2
  cached_network_image: ^3.4.1
  image_picker: ^1.1.2

  # --- In-app camera capture (live preview + record), with a colour-filter
  # strip applied to the preview. Recording still writes the raw take (baking
  # a filter into the file needs ffmpeg, deferred); the OS picker path in the
  # composer stays as the reliable fallback. ---
  camera: ^0.11.0+2

  # --- Google sign-in reuses the backend's web OAuth (/api/auth/google/start
  # -> Google -> callback redirects to /#auth=<token>); a WebView drives that
  # flow and captures the token from the final redirect. Requires the Worker's
  # GOOGLE_CLIENT_ID env to be set (the endpoint 501s until then). ---
  webview_flutter: ^4.10.0

  # --- WhatsApp-style instant-render local cache (stale-while-revalidate) ---
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5

  # --- Tappable links inside chat messages ---
  url_launcher: ^6.3.1

  # --- Push notifications that arrive while the app is closed. FCM is the only
  # delivery channel Android actually honours in the background; the Worker
  # signs a service-account JWT and posts to the FCM HTTP v1 API (see
  # sendFcm in src/index.js). Requires android/app/google-services.json. ---
  firebase_core: ^3.8.1
  firebase_messaging: ^15.1.6

  # --- Native share sheet (shows installed apps: WhatsApp, Facebook, …) and
  # saving a downloaded video into the phone's gallery ---
  share_plus: ^10.1.4
  gal: ^2.3.1

  # --- Instagram-style autoplay: play the public-feed video only while it's
  # actually on screen, pause it the moment it scrolls away ---
  visibility_detector: ^0.4.0+2

  # --- Voice notes in chat (backend already accepts kind=audio) ---
  # Pinned to the latest major (not ^5.2.1): that version's own transitive
  # record_linux/record_platform_interface combo failed to compile
  # ("RecordLinux missing startStream") even though we don't target Linux —
  # flutter build still type-checks every federated platform impl reachable
  # from the package, so a broken one anywhere in the graph fails the build.
  record: ^7.1.1
  just_audio: ^0.9.42

  # --- Grabs the first frame of a recorded/picked video as a JPEG, so an
  # upload carries a poster (the web app does the same in makeThumb()). Without
  # it the profile grid cell and the feed's pre-playback frame stay black.
  # Using the *_plus fork: the original video_thumbnail's android/build.gradle
  # still calls jcenter() and has no namespace, which fails the modern
  # AGP/Gradle build outright. ---
  video_thumbnail_plus: ^0.0.2
  # Burns the moving Belople watermark + outro into a DOWNLOADED video only
  # (see more_options_sheet _saveVideo). The _min_gpl variant is the smallest
  # build that still ships x264 (H.264 encoding) — we only need core filters
  # (overlay/concat/scale) plus x264, not the full codec set. Requires
  # minSdk 24 (bumped in android/app/build.gradle.kts).
  ffmpeg_kit_flutter_new_min_gpl: ^2.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  build_runner: ^2.4.13
  riverpod_generator: ^2.6.3

  # Generates the Android/iOS launcher icons from the Belople logo.
  flutter_launcher_icons: ^0.14.1

# App launcher icon config — run `dart run flutter_launcher_icons` to
# regenerate after changing assets/icon/belople_logo.jpg.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/belople_logo.jpg"
  min_sdk_android: 21
  remove_alpha_ios: true
  # Adaptive icon: the logo already sits on black, so match the background.
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: "assets/icon/belople_logo.jpg"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # The logo is loaded at runtime to paint the download outro/watermark
  # (VideoWatermarkService) — not just used by flutter_launcher_icons.
  # belople_mark.jpg is the logo on black used for the moving watermark.
  assets:
    - assets/icon/belople_logo.jpg
    - assets/icon/belople_mark.jpg
    # The branded 2s outro clip appended to every downloaded video.
    - assets/video/outro.mp4

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
